const result = function(x){
    return x*x;
}

// arrow function , kalau misal parameternya cuma satu, x nya ga perlu di kurung
const result1 = (x) =>{
    return x*x;
}

// ga pakai return tapi kurung {} ganti pakai (), 1 baris aja yh
const random = () => (
    Math.floor(Math.random()*100)
);

const add = (a,b) => a+b;

